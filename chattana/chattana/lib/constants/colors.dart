import 'dart:ui';
import 'package:flutter/material.dart';

class CustomColors {
  static const Color primary = Color(0xFF10A37F);
  static const Color background = Colors.white; // Arka plan beyaz
  static final Color lightGrey = Colors.white.withOpacity(0.02);
  static final Color darkGrey = Colors.white.withOpacity(0.08);

  static const Color redText = Colors.red;
  static const Color redBubble = Colors.red;
  static const Color pinkBubble = Color(0xFFFFC0CB);
  static const Color lightRedBubble =
      Color(0xFFFF8A80); // Yeni renk: Açık kırmızı
  static const Color sendButton = Colors.red;
}
